<div class="grid">

  <!-- sidebar navigation menu -->
  <div class="grid__item one-quarter small--hide">
    {% include "navigation-menu" %}
  </div>

  <!-- page content -->
  <div class="grid__item three-quarters small--one-whole">
    <div class="text-center">
      <h1>{{ page.title }}</h1>
    </div>
    <div class="rte">
      {{ page.content }}
    </div>
  </div>
</div>

<div class="shop.refund_policy.title">
  {{ shop.refund_policy.title }}
</div>
<div class="shop.refund_policy.body">
  {{ shop.refund_policy.body }}
</div>
<!-- ORIGINAL
<h1>{{ page.title }}</h1>
<div class="rte">
  {{ page.content }}
</div>
-->
